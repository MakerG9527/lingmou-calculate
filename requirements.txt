# 灵眸智算 Demo v2 依赖
# ---- 核心（必装）----
sympy>=1.12
gradio

# ---- 大模型建模（OpenAI 兼容接口；现在用 API，未来可指向本地推理 server）----
openai>=1.0

# ---- 可选：公式 OCR 快速通道（保留上一代识别能力，非主路径）----
# 若要演示"拍照/截图公式 → 识别"，再装下面两项；纯文本建模无需安装。
# pix2text
# pillow

# ---- 可选：若建模器需要直接吃 LaTeX，再装（注意 antlr 版本锁定）----
# latex2sympy2==1.5.4
# antlr4-python3-runtime==4.7.2
